# Copilot Instructions

## Project Guidelines
- User prefers environment switching and test data (URL and credentials) to be handled via appsettings/configuration for CI, not through TestConfiguration static environment resolution logic.